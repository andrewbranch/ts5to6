{
  "name": "@andrewbranch/ts-fix-baseurl",
  "version": "1.0.2",
  "description": "CLI to migrate TypeScript projects away from `baseUrl` usage",
  "license": "MIT",
  "author": "Andrew Branch",
  "type": "module",
  "bin": {
    "ts-fix-baseurl": "dist/cli.js"
  },
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -b src/tsconfig.json",
    "test": "node --experimental-strip-types --test ./test/**/*.test.ts",
    "test:watch": "node --experimental-strip-types --test --watch ./test/**/*.test.ts",
    "cli": "node --experimental-strip-types ./src/cli.ts",
    "fmt": "dprint fmt",
    "fmt:check": "dprint check",
    "postinstall": "patch-package",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "chalk": "^5.6.0",
    "glob": "^11.0.3",
    "patch-package": "^8.0.0",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "dprint": "^0.50.1",
    "ts-expose-internals": "^5.6.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
