// Import from shared package using path mapping (inherited from base config)
import { Product, User } from "@shared/types";
import { formatCurrency } from "@shared/utils";

// Import from local modules using baseUrl (inherited from base config)
import {} from "packages/client/src/ClientHelper";
