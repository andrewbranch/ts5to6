// Import from shared package using own path mapping
import { Product, User } from "@shared/types";
import { formatCurrency } from "@shared/utils";

// Import from local modules using inherited baseUrl
import {} from "packages/server/src/ServerService";
